name: Debug
on: workflow_dispatch
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo `date "+%b %d"` - `date -d +5d "+%b %d"`
      - run: echo $FUNK
        env:
          FUNK: funky
