name: Debug
on: workflow_dispatch
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo `date "+%b %d"` - `date -d "+5 days" "+%b %d"`
      - run: echo $FUNK
        env:
          FUNK: funky
