name: Debug
"on":
  repository_dispatch: {}
  workflow_dispatch: {}
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      # this is not a real token don't you worry
      - uses: gr2m/hello-world-js-action@v1.x
